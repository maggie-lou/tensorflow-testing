actions:
  - name: Build TF
    triggers: { push: { branches: [ "*" ] } } # run when pushing any branch
    bazel_commands:
      - bazel clean
      - bazel build --config=dev //tensorflow:tensorflow --remote_upload_local_results
